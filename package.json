{
  "name": "engram",
  "version": "0.1.0",
  "description": "Memory evolution system for AI agents - System 2 to System 1 thinking through Hegelian dialectic",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "hook": "bun run scripts/engram-hook.ts",
    "worker": "bun run scripts/start-worker.ts",
    "worker:stop": "bun run scripts/stop-worker.ts",
    "worker:status": "bun run scripts/worker-status.ts",
    "db:stats": "sqlite3 data/engram.db \"SELECT 'memories' as table_name, COUNT(*) as count FROM memories UNION SELECT 'patterns', COUNT(*) FROM patterns UNION SELECT 'theses', COUNT(*) FROM theses UNION SELECT 'syntheses', COUNT(*) FROM syntheses\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.29",
    "@huggingface/transformers": "^3.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
